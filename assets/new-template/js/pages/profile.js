$(function() {
    // Load Form on dialog open.
    $("#dialogSelectPayment").on("show.bs.modal", function(e) {
        //var link = $(e.relatedTarget);
        //$(this).find(".modal-body").load(link.attr("href"));
    });

});
